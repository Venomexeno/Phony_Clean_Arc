import 'package:flutter/material.dart';

class DeviceSpecScreen extends StatefulWidget {
  const DeviceSpecScreen({Key? key}) : super(key: key);

  @override
  State<DeviceSpecScreen> createState() => _DeviceSpecScreenState();
}

class _DeviceSpecScreenState extends State<DeviceSpecScreen> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
